# Legacy Static Site

This directory holds the original static-site implementation that predated the
current Vite/React marketing property.

It is preserved for reference only.

These files are not part of the current production architecture:

- `privacy.html`
- `script.js`
- `site-config.js`
- `styles.css`

The active marketing property now lives in:

- `src/`
- `public/`
- `index.html`
